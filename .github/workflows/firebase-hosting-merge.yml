# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies
        run: npm ci
        working-directory: my-lab-app
      - name: Build frontend
        env:
          VITE_API_BASE_URL: ${{ secrets.VITE_API_BASE_URL || 'https://lab-app-r7ao4jqdya-ue.a.run.app/api' }}
        run: |
          echo "Environment variable VITE_API_BASE_URL=$VITE_API_BASE_URL"
          cd my-lab-app
          echo "VITE_API_BASE_URL=$VITE_API_BASE_URL" > .env.production
          cat .env.production
          npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_MY_LAB_APP_3512A }}
          channelId: live
          projectId: my-lab-app-3512a
          entryPoint: "./my-lab-app"
